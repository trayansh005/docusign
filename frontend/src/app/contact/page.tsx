import ContactClient from './ContactClient';

export const metadata = {
    title: 'Contact Us - FomiqSign Platform',
    description: 'Get in touch with our team for support, questions, or feedback about FomiqSign Platform.',
};

export default function ContactPage() {
    return <ContactClient />;
}